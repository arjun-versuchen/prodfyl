import { distributeQuestions } from '../../questionFactory'
import type { QuestionInput } from '../../questionFactory'

export const azureDataFactoryQuestions = distributeQuestions(
  'azure-data-factory',
  [
    {
      sheet: 'adf-pipelines',
      category: 'Pipelines',
      items: [
        {
          title: 'Pipeline structure and activities',
          difficulty: 'Easy',
          frequency: 'Very High',
          tags: ['pipeline', 'activities'],
          question: 'What is an ADF pipeline and how do activities relate to each other?',
          answer: 'A pipeline is a logical grouping of activities that perform a unit of work—ingest, transform, or publish. Activities connect via dependsOn for ordering; success/failure/completion paths control branching. A single pipeline run shares parameters and variables. Complex workflows split into nested pipelines invoked via Execute Pipeline activity for modularity and reuse.',
        },
        {
          title: 'Activity dependency types',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['dependencies', 'control-flow'],
          question: 'Explain activity dependency conditions: Succeeded, Failed, Skipped, and Completed.',
          answer: 'Succeeded runs when prior activity succeeds—default for sequential chains. Failed triggers error-handling branches. Skipped runs when upstream skipped. Completed runs regardless of success or failure—useful for cleanup in Finally-style patterns. Combine multiple dependencies; all must satisfy their condition before the downstream activity starts.',
        },
        {
          title: 'Execute Pipeline activity',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['nested-pipeline', 'modularity'],
          question: 'Why use Execute Pipeline instead of one monolithic pipeline?',
          answer: 'Nested pipelines enable reusable templates (ingest per source), independent testing, parallel fan-out per entity, and clearer Monitor views. Pass parameters to child pipelines for source-specific config. Reduces canvas complexity and allows team ownership per domain pipeline.',
        },
        {
          title: 'Pipeline return values',
          difficulty: 'Medium',
          frequency: 'Medium',
          tags: ['output', 'parameters'],
          question: 'How do you pass data between activities in ADF?',
          answer: 'Activity outputs expose JSON via @activity(\'Name\').output—e.g., Lookup rowsets, Copy rowsCopied, SetVariable results. Use expressions in subsequent activity properties. For large datasets, write to staging storage or control table rather than passing inline JSON. Pipeline returnValue can surface final status to parent pipeline.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-activities',
      category: 'Activities',
      items: [
        {
          title: 'Copy activity internals',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['copy', 'ingestion'],
          question: 'How does the Copy activity achieve high throughput?',
          answer: 'Copy uses parallel DIUs (Data Integration Units) to partition source reads and sink writes. Enable partition option on relational sources, tune parallelCopies, and use PolyBase or COPY INTO for Synapse sink. Avoid data flow for simple copies—Copy is optimized and cheaper. Self-hosted IR near source reduces latency.',
        },
        {
          title: 'Mapping Data Flow vs Copy',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['data-flow', 'transform'],
          question: 'When do you use Mapping Data Flow instead of Copy activity?',
          answer: 'Data Flows provide visual Spark-based transforms—joins, aggregates, derived columns, upserts—without external compute. Use for moderate transform logic inside ADF. For heavy ETL or complex code, Execute Databricks/Synapse notebook instead—more flexible and often cheaper at scale. Copy alone handles movement without transformation.',
        },
        {
          title: 'ForEach activity patterns',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['foreach', 'parallelism'],
          question: 'Explain ForEach activity sequential vs parallel batch count.',
          answer: 'ForEach iterates over array from pipeline parameter or Lookup output. Sequential processes one item at a time—safe for rate-limited APIs. Parallel batch count runs N concurrent iterations—faster but risks throttling source/sink. Set batchCount based on IR capacity and target limits; use @item() to pass current element to child activities.',
        },
        {
          title: 'Lookup and Stored Procedure activities',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['lookup', 'stored-proc'],
          question: 'What are common uses of Lookup activity in production pipelines?',
          answer: 'Fetch watermark values, config rows, file lists, or API metadata before main processing. Lookup returns small datasets (max 5000 rows, 4 MB). Chain to If Condition or ForEach. Stored Procedure activity executes SQL for MERGE, watermark update, or audit logging—often in Finally block after Copy.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-linked-services',
      category: 'Linked Services',
      items: [
        {
          title: 'Linked service purpose',
          difficulty: 'Easy',
          frequency: 'Very High',
          tags: ['linked-service', 'connections'],
          question: 'What is a linked service and how does it differ from a dataset?',
          answer: 'Linked service defines connection information to external resources—server name, database, auth method—like a connection string template. Dataset defines structure and location of data within that linked service—table name, file path, schema. Multiple datasets can reference one linked service.',
        },
        {
          title: 'Authentication options',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['auth', 'managed-identity'],
          question: 'What authentication methods does ADF support for Azure SQL and ADLS?',
          answer: 'Managed identity (recommended), service principal with Key Vault secret, SQL auth (discouraged), and system/user-assigned MSI for Azure resources. For on-prem SQL, use self-hosted IR with Windows auth or SQL auth stored in Key Vault. Enable Azure AD-only auth on SQL; grant ADF MSI db_datareader/writer roles.',
        },
        {
          title: 'Key Vault linked service',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['key-vault', 'secrets'],
          question: 'How do you reference Key Vault secrets in linked services?',
          answer: 'Create Azure Key Vault linked service with MSI auth. In linked service connection string or password field, use AzureKeyVaultSecret type referencing secret name and version. ADF resolves at runtime. Rotating secrets in Vault automatically applies on next pipeline run without redeploying ARM templates if version omitted.',
        },
        {
          title: 'Parameterized linked services',
          difficulty: 'Hard',
          frequency: 'Medium',
          tags: ['parameterization', 'dynamic'],
          question: 'Can linked services be dynamic per environment without duplicating factories?',
          answer: 'Use global parameters or ARM template parameters for environment-specific connection properties at deploy time. For runtime dynamic connections, parameterize dataset paths and use single linked service with wildcard, or Execute Pipeline per tenant with different linked service names via global params. CI/CD deploys separate factory instances per env is most common pattern.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-datasets',
      category: 'Datasets',
      items: [
        {
          title: 'Dataset parameters',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['dataset', 'parameters'],
          question: 'Why parameterize datasets and give an example.',
          answer: 'Parameterized datasets reuse one definition for multiple tables or paths—e.g., schema and table as parameters passed from pipeline. Enables generic ingest pipeline: @dataset().schemaName, @dataset().tableName in Azure SQL dataset. Reduces object sprawl from hundreds of static datasets.',
        },
        {
          title: 'Binary vs DelimitedText vs Parquet datasets',
          difficulty: 'Easy',
          frequency: 'High',
          tags: ['formats', 'datasets'],
          question: 'When do you use Binary dataset vs format-specific datasets?',
          answer: 'Binary copies files byte-for-byte without parsing—ideal for PDFs, images, or any file as-is. DelimitedText and Parquet datasets parse schema for column mapping in Copy and Data Flow. Use Binary for raw landing; format datasets when schema mapping or column projection is needed.',
        },
        {
          title: 'Schema drift in datasets',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['schema', 'drift'],
          question: 'How does ADF handle schema mismatch between source and sink datasets?',
          answer: 'Copy activity mapping tab maps columns explicitly or imports from source. Enable fault tolerance to skip incompatible rows. For flexible sinks, use AutoCreateTable on Synapse or write to JSON. Data Flow schema drift options allow extra/missing columns handling. Validate with GetMetadata activity before copy.',
        },
        {
          title: 'GetMetadata activity',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['metadata', 'validation'],
          question: 'How do you use GetMetadata to validate files before processing?',
          answer: 'GetMetadata on folder dataset with fieldList childItems, itemName, lastModified retrieves file list and properties. Chain If Condition to check file exists and is non-empty before Copy. Useful for event-driven pipelines expecting daily drop—fail fast with clear alert if file missing.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-ir',
      category: 'Integration Runtime',
      items: [
        {
          title: 'Azure IR vs self-hosted IR',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['integration-runtime', 'networking'],
          question: 'When must you use self-hosted Integration Runtime?',
          answer: 'Self-hosted IR required when ADF must reach data inside private network—on-prem SQL, Oracle, file shares, or VNet-isolated resources without public endpoints. Install IR on VM or scale-out cluster near data source. Azure IR handles cloud-to-cloud copies natively without agent.',
        },
        {
          title: 'Azure-SSIS IR',
          difficulty: 'Medium',
          frequency: 'Medium',
          tags: ['ssis', 'lift-shift'],
          question: 'What is Azure-SSIS Integration Runtime used for?',
          answer: 'Hosts SSIS packages lifted from SQL Server for execution in Azure without rewriting to ADF Data Flows. Supports package deployment model, custom components, and Enterprise features. Provision SSIS IR in VNet; use SSISDB on Azure SQL. Migration path for legacy ETL while modernizing incrementally.',
        },
        {
          title: 'IR performance tuning',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['performance', 'ir'],
          question: 'Copy from on-prem SQL is slow. How do you tune self-hosted IR?',
          answer: 'Scale out IR nodes for parallel copies. Ensure network bandwidth and low latency to source. Increase Copy parallelCopies and DIUs. Partition source query. Run IR on VM sized for CPU/memory; avoid oversubscribed host. Check firewall rules, TLS overhead, and source DB indexing on filter columns.',
        },
        {
          title: 'Managed VNet IR',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['vnet', 'managed-ir'],
          question: 'Explain Managed Virtual Network Integration Runtime.',
          answer: 'Managed VNet IR runs copy and Data Flow activities inside Microsoft-managed network isolated from public internet—required for connecting to private-linked Azure resources without self-hosted IR. Managed private endpoints connect to ADLS/SQL securely. Use when data exfiltration policies block standard Azure IR egress.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-triggers',
      category: 'Triggers',
      items: [
        {
          title: 'Schedule trigger vs tumbling window',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['triggers', 'scheduling'],
          question: 'Compare schedule trigger and tumbling window trigger.',
          answer: 'Schedule trigger fires pipeline on cron-like cadence—simple daily/hourly jobs. Tumbling window trigger processes fixed-size time slices with built-in retry, dependency on prior window, and payload @trigger().startTime/@endTime—ideal for idempotent hourly/daily partitions and backfill. Tumbling window supports max concurrency and self-dependency for chained windows.',
        },
        {
          title: 'Event-based triggers',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['event-trigger', 'storage'],
          question: 'How do storage event triggers work in ADF?',
          answer: 'BlobCreated or BlobDeleted events from ADLS/Blob via Event Grid subscription start pipeline with folder path in trigger payload. Filter by prefix/suffix to scope file types. Enables near-real-time ingestion on file landing. Ensure idempotent processing—events may duplicate.',
        },
        {
          title: 'Trigger dependency chains',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['tumbling-window', 'dependencies'],
          question: 'How do you ensure pipeline B runs only after pipeline A completes for the same window?',
          answer: 'Use tumbling window trigger with dependsOn referencing another trigger—ADF waits for referenced trigger\'s window success before firing. Alternatively, single orchestrator pipeline calls both in sequence. For cross-factory dependencies, use external event or control table polling pattern.',
        },
        {
          title: 'Manual vs automated trigger management',
          difficulty: 'Easy',
          frequency: 'Medium',
          tags: ['operations', 'triggers'],
          question: 'What happens when you stop a trigger during deployment?',
          answer: 'Triggers must be stopped before ARM template deployment updating trigger-dependent resources—hot deployment may fail if active. Use pre/post deployment scripts in Azure DevOps to Stop-Trigger, deploy, Start-Trigger. Stopped triggers retain definition but do not fire; in-flight runs continue.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-parameters',
      category: 'Parameters',
      items: [
        {
          title: 'Pipeline vs trigger parameters',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['parameters', 'triggers'],
          question: 'How do pipeline parameters differ from trigger parameters?',
          answer: 'Pipeline parameters define inputs the pipeline accepts at invocation—defaults set in pipeline JSON. Trigger parameters map values when trigger fires, including tumbling window start/end. Invoke pipeline via REST passes parameter overrides. Use expressions @pipeline().parameters.paramName in activities.',
        },
        {
          title: 'Global parameters',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['global-parameters', 'cicd'],
          question: 'What are global parameters and when are they useful?',
          answer: 'Factory-level parameters shared across all pipelines—environment name, lake root path, Key Vault URL. Set per environment during ARM deployment without editing each pipeline. Referenced as @pipeline().globalParameters.param in expressions. Simplifies CI/CD promoting same JSON across dev/test/prod.',
        },
        {
          title: 'Expression language basics',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['expressions', 'dynamic'],
          question: 'Write an expression to build a dynamic file path with date parameter.',
          answer: 'Use concat and formatDateTime: @concat(\'raw/sales/\', formatDateTime(pipeline().parameters.runDate, \'yyyy/MM/dd\'), \'/data.parquet\'). Pipeline parameters typed as string or datetime. Escape quotes in JSON. Functions include addDays, replace, split, coalesce for null handling.',
        },
        {
          title: 'Parameterizing copy source query',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['dynamic-sql', 'incremental'],
          question: 'How do you parameterize a SQL source query for incremental load?',
          answer: 'In Azure SQL source dataset, set sqlReaderQuery with expression: @concat(\'SELECT * FROM dbo.Orders WHERE ModifiedDate > \'\', \'\', pipeline().parameters.watermark, \'\'). Better: use sqlReaderStoredProcedureQueryOption or parameterized stored proc to avoid SQL injection. Update watermark in control table post-success via Stored Procedure activity.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-variables',
      category: 'Variables',
      items: [
        {
          title: 'Variables vs parameters',
          difficulty: 'Easy',
          frequency: 'High',
          tags: ['variables', 'parameters'],
          question: 'What is the difference between pipeline variables and parameters?',
          answer: 'Parameters are external inputs set at invocation—read-only inside run. Variables are mutable internal state modified by SetVariable and AppendVariable activities during execution. Use variables for counters, accumulated error messages, or runtime flags set by If Condition branches. Parameters cannot change mid-pipeline.',
        },
        {
          title: 'SetVariable and AppendVariable',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['setvariable', 'append'],
          question: 'Give a use case for AppendVariable in a pipeline.',
          answer: 'Inside ForEach processing files, AppendVariable builds comma-separated list of failed file names on error branch. After ForEach, If Condition checks variable length > 0 to send alert email with full list. Array variables support append; scalar variables use SetVariable overwrite.',
        },
        {
          title: 'Variable scope in ForEach',
          difficulty: 'Hard',
          frequency: 'Medium',
          tags: ['foreach', 'scope'],
          question: 'Can ForEach iterations share and update the same pipeline variable safely?',
          answer: 'Parallel ForEach iterations racing to SetVariable cause lost updates—avoid shared mutable state in parallel mode. Use sequential ForEach for accumulation, or write iteration results to temp table/sink and aggregate afterward. AppendVariable in parallel ForEach is not thread-safe; design for idempotent per-iteration outputs.',
        },
        {
          title: 'Using variables in expressions',
          difficulty: 'Medium',
          frequency: 'Medium',
          tags: ['expressions', 'variables'],
          question: 'How do you reference a variable in a downstream activity property?',
          answer: 'Syntax: @variables(\'varName\'). Use in dynamic dataset paths, If Condition expressions, or Web activity body. Initialize variables in pipeline variables tab with default value. Type must match—String, Boolean, Array. Coalesce with pipeline parameters when variable may be unset.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-error-handling',
      category: 'Error Handling',
      items: [
        {
          title: 'Retry policy configuration',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['retry', 'resilience'],
          question: 'How do you configure retry policy on ADF activities?',
          answer: 'Each activity has policy.timeout and policy.retry: count, intervalInSeconds, secureInput/output flags. Default retry handles transient faults—network blips, throttling. Set count 3–5 with exponential backoff for API calls. Long-running Copy may need higher timeout (7 days max). Retry does not help logic errors—fix mapping instead.',
        },
        {
          title: 'Try-catch pattern in ADF',
          difficulty: 'Hard',
          frequency: 'Very High',
          tags: ['error-handling', 'patterns'],
          question: 'Implement try-catch-finally pattern in ADF pipelines.',
          answer: 'Main activity dependsOn success chain. Parallel failure branch: same activity Failed dependency → SetVariable/log error → Web activity notify. Finally: activity with Completed dependency on main (and catch) runs cleanup—e.g., update control table status. Execute Pipeline wraps try-catch for reusable error module.',
        },
        {
          title: 'Dead-letter and quarantine patterns',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['dead-letter', 'quarantine'],
          question: 'How do you route bad records without failing the entire pipeline?',
          answer: 'Copy activity fault tolerance: skip incompatible rows, log to blob quarantine path. Data Flow assert with split to error stream. Set pipeline to succeed with warning if error row count below threshold via Lookup validation post-copy. Monitor quarantine folder; replay after fix.',
        },
        {
          title: 'Alerting on pipeline failure',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['alerting', 'monitor'],
          question: 'How do you alert teams when ADF pipeline fails?',
          answer: 'Azure Monitor alert rule on ADF PipelineRuns metric FailedRuns > 0, or Log Analytics query on failure status. Action group sends email, Teams, or Logic App webhook. ADF built-in alerts in Monitor hub. Include pipeline name, run ID, and error message in custom Web activity notification for context.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-incremental',
      category: 'Incremental Loads',
      items: [
        {
          title: 'Watermark incremental pattern',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['watermark', 'incremental'],
          question: 'Describe the watermark column pattern for incremental loads in ADF.',
          answer: 'Store last processed high-watermark value in control table. Lookup retrieves watermark; Copy source query filters WHERE modified_column > @watermark. After success, Stored Procedure updates watermark to MAX(modified_column) from batch. Use datetime or incremental int keys; handle clock skew with small overlap and dedupe on merge.',
        },
        {
          title: 'Change Data Capture with ADF',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['cdc', 'sql-server'],
          question: 'How do you ingest SQL Server CDC changes using ADF?',
          answer: 'Enable CDC on source table. Use Copy with sqlReaderQuery against cdc.fn_cdc_get_all_changes_* or incremental from CDC LSN stored in control table. Alternative: Azure SQL MI link or Debezium to Event Hubs then ADF. Merge changes into Delta/Synapse sink with MERGE activity via stored proc.',
        },
        {
          title: 'Full vs incremental tradeoffs',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['strategy', 'incremental'],
          question: 'When is full reload acceptable instead of incremental load?',
          answer: 'Small dimension tables (< millions rows), sources without reliable change column, or weekly reconciliation loads. Full reload simplifies logic—truncate and load. Incremental required for large fact tables, strict source load limits, and near-real-time SLAs. Hybrid: incremental daily plus full reconcile monthly.',
        },
        {
          title: 'Upsert to Delta Lake from ADF',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['upsert', 'delta'],
          question: 'How do you perform upserts from ADF into Delta Lake?',
          answer: 'ADF Copy alone appends—use Databricks notebook activity with MERGE INTO on key columns, or Mapping Data Flow with alter row upsert if sink supports it. Pass staging path from Copy, then notebook merges staging to Delta. Idempotent on natural or surrogate key; handle deletes with merge condition or soft-delete flag.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-monitoring',
      category: 'Monitoring',
      items: [
        {
          title: 'Monitor hub overview',
          difficulty: 'Easy',
          frequency: 'Very High',
          tags: ['monitor', 'adf'],
          question: 'What can you see in ADF Monitor hub?',
          answer: 'Pipeline, activity, and trigger run history with status, duration, and error details. Filter by date, rerun failed pipelines, cancel in-flight runs. Gantt view shows activity timeline. Integration runtime monitoring for CPU and concurrent jobs. Open run to inspect input/output JSON for debugging expressions.',
        },
        {
          title: 'Diagnostic settings and Log Analytics',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['log-analytics', 'diagnostics'],
          question: 'How do you send ADF logs to Log Analytics for custom dashboards?',
          answer: 'Enable diagnostic settings on factory—send ActivityRuns, PipelineRuns, TriggerRuns, SSIS logs to Log Analytics workspace. Build KQL workbooks for failure trends, slowest activities, and SLA compliance. Retention and cost controlled by workspace SKU. Correlate with Databricks logs via runId custom field.',
        },
        {
          title: 'Pipeline run analytics',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['analytics', 'metrics'],
          question: 'How do you track pipeline duration trends over time?',
          answer: 'Query PipelineRuns in Log Analytics: summarize avg(duration_s), percentile(duration_s, 95) by pipelineName_s, bin(TimeGenerated, 1d). Azure Monitor metrics API for FailedRuns and SucceededRuns. Set alert when p95 exceeds baseline—often indicates source slowdown or data volume growth needing scale-up.',
        },
        {
          title: 'Debugging failed activities',
          difficulty: 'Medium',
          frequency: 'Very High',
          tags: ['debugging', 'troubleshooting'],
          question: 'Walk through debugging a Copy activity failure.',
          answer: 'Open failed run → Copy activity error message (auth, timeout, schema). Check IR connectivity—Test connection on linked service. Validate dataset path exists via Storage Explorer. Review Copy detailed log in Monitor for rows skipped vs failed. Re-run debug in isolation with same parameters; use Execute Pipeline with write batch size 1 for pinpoint.',
        },
        {
          title: 'Cost monitoring for ADF',
          difficulty: 'Medium',
          frequency: 'Medium',
          tags: ['cost', 'monitoring'],
          question: 'How do you monitor ADF spending?',
          answer: 'Azure Cost Management filter by resource Microsoft.DataFactory/factories. ADF Analytics shows activity count and Data Flow vCore hours. Tag factories by project. Alert on monthly budget threshold. Optimize high-cost Mapping Data Flows by migrating to Databricks; reduce debug IR usage in production subscriptions.',
        },
      ] satisfies QuestionInput[],
    },
    {
      sheet: 'adf-scenarios',
      category: 'Scenario Questions',
      items: [
        {
          title: 'Lift-and-shift SSIS to ADF',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['ssis', 'migration'],
          question: 'Scenario: Migrate 200 SSIS packages to Azure. What is your strategy?',
          answer: 'Assess with SSIS Migration Assessment tool. Categorize: run as-is on Azure-SSIS IR, refactor to ADF pipelines, or rewrite to Databricks. Prioritize by complexity and change frequency. Deploy SSISDB to Azure SQL, lift packages to SSIS IR in VNet. Incrementally replace stable batch packages with native ADF for lower ops cost.',
        },
        {
          title: 'Duplicate pipeline runs on file trigger',
          difficulty: 'Hard',
          frequency: 'Very High',
          tags: ['idempotency', 'event-trigger'],
          question: 'Scenario: Storage event trigger causes duplicate processing of the same file. Fix it.',
          answer: 'Event Grid may deliver at-least-once. Track processed file ETag or name hash in control table; Lookup before Copy skips if exists. Use Delta MERGE idempotent on file key. Alternatively, move file to processing/ then archive/ after success. Reduce trigger scope with precise prefix filter to avoid partial upload triggers.',
        },
        {
          title: 'Cross-region disaster recovery',
          difficulty: 'Hard',
          frequency: 'Medium',
          tags: ['dr', 'geo-redundancy'],
          question: 'Scenario: Design ADF DR for pipelines orchestrating multi-region data.',
          answer: 'Deploy secondary factory in paired region via ARM CI/CD; replicate Git repo. ADLS GRS for data; secondary IR in DR VNet. Document manual or automated failover: redirect triggers to secondary factory, update global parameters for region-specific endpoints. Test failover quarterly; RPO equals replication lag plus last successful watermark.',
        },
        {
          title: 'Rate-limited REST API ingestion',
          difficulty: 'Hard',
          frequency: 'High',
          tags: ['api', 'throttling'],
          question: 'Scenario: Ingest paginated REST API with strict rate limits into ADLS daily.',
          answer: 'Until activity loops pages with Wait activity delay between calls respecting Retry-After header. Store pagination token in variable. Sequential ForEach over endpoints—not parallel. On 429, retry policy with backoff. Land JSON per page, merge in downstream Spark job. Cache static reference data to reduce calls.',
        },
        {
          title: 'ARM template deployment failure',
          difficulty: 'Medium',
          frequency: 'High',
          tags: ['cicd', 'deployment'],
          question: 'Scenario: ADF ARM deployment fails because triggers are active. How do you fix CI/CD?',
          answer: 'Add pre-deployment PowerShell: Stop-AzDataFactoryTrigger for each trigger. Deploy ARM template. Post-deployment: Start-AzDataFactoryTrigger. Use Azure DevOps ADF publish branch workflow or Microsoft-provided npm package adf-deploy. Include smoke test pipeline run in staging before prod trigger start.',
        },
      ] satisfies QuestionInput[],
    },
  ],
  1300,
)
